\section{Introduction}%
\label{sec:introduction}
%Each comment introduces 1-2 sentences 
% Context 
%{\color{red}
%Knowledge graphs (KGs) play a vital role in enabling semantic interoperability,
%integration, and querying. 
%They allow systems to execute advanced semantic queries, facilitating the
%integration and reasoning of the data at hand. 
%The importance of KGs is further amplified by the growing adoption of large language
%models (LLMs) alongside KGs for tasks like retrieval-augmented
%generation~\cite{Xu2024RAGKGLinkedin}. 
%However, many existing data infrastructures lack the capability to generate KGs
%dynamically, and migrating legacy databases with diverse data formats to 
%KG-based systems can be costly for organizations.
%Unlocking the wealth of knowledge in these legacy systems require the generation 
%of KGs from heterogeneous data efficiently. 
%One such approach of generating KGs involves the usage of declarative mapping
%languages~\cite{VanAssche2022DeclarativeRDFgraph}. 
%
%Application of declarative mapping languages has gained traction in recent years across
%diverse domains such as COVID research~\cite{Sakor2023KG4Covid}, railway
%networks~\cite{rojas2021leveraging}, and incident management in ICT
%systems~\cite{Tailhardat2024NORIA}. 
%A plethora of mapping languages exist to support these practical applications. 
%On one hand, query-based languages like SPARQL-Generate~\cite{lefrancois_eswc_2017} aim to
%extend SPARQL for KG generation from heterogeneous data. 
%On the other hand, dedicated mapping languages, such as the RDF Mapping Language
%(RML)~\cite{dimou_ldow_2014}, use custom syntax. 
%Furthermore, a single mapping language can have multiple implementations for
%generating KGs. For example, RML has several
%implementations~\cite{Freund2024FlexRML,Iglesias2022ScalingKnowledgeGraph,ArenasGuerrero2022MorphKGCScalable,MinOo2022RMLStreamer,Scrocca2020Transport},
%each offering distinct advantages for their own unique use cases (i.e.,
%FlexRML~\cite{Freund2024FlexRML} for KG generation on IoT devices). 
%
%}

Knowledge graphs (KGs) are often created or populated by extracting and transforming data from other sources of structured or semi-structured data. One approach to this end is to
	%specify the desired mapping using a declarative mapping language ...
	employ a mapping engine for which the desired mapping from the source data to the KG can be specified using a declarative language.
		%Several such declarative mapping languages have been 
		This approach has gained traction in recent years, with applications across diverse domains such as COVID research~\cite{Sakor2023KG4Covid}, railway networks~\cite{rojas2021leveraging}, and incident management in ICT systems~\cite{Tailhardat2024NORIA}. To provide a basis for this approach, several declarative mapping languages, in particular for RDF-based KGs, have been proposed,
%
including languages that focus only on a single type of input data sources (e.g., R2RML for relational databases~\cite{Seema2012R2RML}) and, more interestingly, also languages designed for multiple types of input data sources~\cite{VanAssche2022DeclarativeRDFgraph}. The latter can be classified further into mapping-focused extensions of query languages and dedicated languages
	%that have been
designed solely for the declarative description of mappings.%

% what’s the problem
%{\color{red}%
%State-of-the-art mapping engines exhibit varying operational semantics due to
%the lack of well-defined formal foundation in the mapping languages they are
%built upon. 
%This absence of formalizations complicates verifying the correctness of the
%mapping engines and studying the expressiveness of the mapping languages.
%Additionally, it poses challenges in optimizing the mappings described by the
%mapping languages.
%}


So far,
	%the majority
	most
of these mapping languages---in particular, the dedicated ones---are defined only informally. Yet, such an informal definition can easily lead to discrepancies between different
	%implementations of the language.
	%implementations of such a language.
	implementations.
For instance, for
	%the mapping language
RML~\cite{dimou_ldow_2014,Dimou2024:RMLSpec}, which has
	%attracted
several implementations~\cite{ArenasGuerrero2022MorphKGCScalable,Freund2024FlexRML,Iglesias2022ScalingKnowledgeGraph,MinOo2022RMLStreamer,Scrocca2020Transport}, the introduction of
	%a collection of
conformance tests~\cite{Heyvaert2019:RMLTests} has revealed various cases in which
	%one or another implementation fails
	different implementations fail
such a test~\cite{Heyvaert2022:RMLTestReports}. It may be argued that the developers of these implementations
	%did not interpret the informal definition of RML~\cite{Dimou2024:RMLSpec} as intended by the writers of these definitions.
	interpreted the informal definition of the language differently.
	%interpreted the informal definition of RML differently.
Without a formal definition it is impossible to introduce implementation and optimization techniques that are provably correct%
	%\ and it is also impossible
	, or
to
	%study computational properties or properly compare different languages or language features in terms of their expressive power.
	%properly compare fundamental properties of different languages or language features, such as their expressive power.
	properly compare languages or language features in terms of fundamental properties, such as their expressive~power.
	%studying fundamental properties of different languages (e.g., expressive power).
%
%
Therefore, the goal of our work in this paper is to provide a solid formal foundation for such declarative mapping languages. Especially, instead of focusing on a single specific language, we aim for a more general formalization approach.

Our main contribution is a language-agnostic algebra to capture definitions of mappings from heterogeneous types of data sources to RDF-based KGs. The basis of this mapping algebra is a variation of the relational data model~(Section~\ref{sec:data_model}), for which we define five types of operators that can be arbitrarily combined into algebra expressions~(Section~\ref{sec:mapping_algebra}).
%
Due to its language-agnostic nature, the algebra can serve as a foundation to
	%capture the semantics of
	%define
various mapping languages formally. As our second contribution, we demonstrate this benefit by providing an algorithm that translates mappings defined using RML into our algebra~(Section~\ref{sec:expressiveness}). Through this algorithm, we show not only that our algebra is at least as expressive as RML, but we also provide a formal definition of the semantics of RML. To the best of our knowledge, this is the first formal approach to capture the RML semantics.
%
Another important value of having an algebra that captures declarative mapping definitions is that
	%this algebra
	it
may be used as the basis of a systematic and well-defined approach to plan and to optimize
	the execution of
%
	KG construction processes
	%mappings
in mapping engines%
	%, similar to how algebras of query languages are used to capture logical plans in query engines%
.
Related to this option, we make our third contribution: We show several algebraic
	equivalences which can be used as rewriting rules
	%rewriting rules that can be used
to optimize mapping plans that are based on our algebra~(Section~\ref{sec:equivalences}).

% why it’s an interesting problem

% what’s my solution + paper outline
%{\color{red}%
%In this paper, we fill the gap in the lack of formalizations in 
%mapping languages by providing mapping language-agnostic
%formalizations with three contributions.
%The first contribution is the introduction of formal definitions and the proposal
%of a set of algebraic mapping operators.
%These operators can be composed to formally define the mapping of
%heterogeneous data to KG in a language-agnostic manner
%(Sections~\ref{sec:data_model} and~\ref{sec:mapping_algebra}).
%A language-agnostic formalization of mapping heterogeneous data to KG lays the
%foundation for a unified mapping engine that can work with the user's preferred
%mapping language. 
%This approach ensures verified correctness and enables optimizations across
%various mapping languages.
%The second contribution demonstrates the expressiveness of our formalisms by
%applying them to a specific mapping language. 
%Specifically, we present an algorithm that translates RML documents into
%expressions composed of the algebraic operators defined in our formalism
%(Section~\ref{sec:expressiveness}). 
%This algorithm shows that our formalism can represent every mapping definable
%in RML, proving it to be at least as expressive as RML while also providing
%formal semantics for RML. 
%Lastly, as our final contribution, we introduce a few algebraic equivalences
%to show the potential of algebraic expression rewriting and thus, enabling the
%optimizations of mapping heterogeneous data to KG (Section~\ref{sec:equivalences}).
%}


%
% paper outline
%%The paper is outlined as follows.
	 %\todo{I personally find it quite useless and a waste of valuable space to have such a paragraph that simply lists all sections with a brief mention of their respective content. If you think about it, do you ever read these paragraphs?? What I typically do instead, is to integrate the section references into the text that introduces the contributions. As concrete examples, you may look at the following three papers:
	 %https://olafhartig.de/files/ChengHartig_FedQPL_iiWAS2020_Extended.pdf
	 %https://olafhartig.de/files/ChengHartig_OptPlus_2019.pdf
	 %https://olafhartig.de/files/ChengHartig_LinGBM_WISE2022_AcceptedManuscript.pdf
	 %}
%Section~\ref{sec:related_works} provides an overview of related work in the domain of  
%formalizing declarative mapping languages. 
%Section~\ref{sec:data_model} introduces the notations and symbols we 
%used to provide the formal definitions of algebraic mapping operators. 
%Section~\ref{sec:mapping_algebra} contains the formal definitions of 
%our algebraic mapping operators and their semantics. 
%In Section~\ref{sec:rml_translation} we provide our translation algorithm
%for transforming RML document into an expression composed of our algebraic 
%mapping operators, thereby formalizing RML. 
%Section~\ref{sec:equivalences} introduces a few equivalences and proofs for 
%optimizations of the generated mapping plan. 
%Finally, we conclude this paper and provides possible future works utilizing
%our algebraic mapping operators in Section~\ref{sec:conclusion}. 

