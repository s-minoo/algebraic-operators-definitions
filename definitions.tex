\section{Algebraic Mapping Operators}%
\label{sec:algebraic_mapping_operators}

\subsection{Source Operator}%
\label{sub:source_operator}

\subsubsection{Notations}%
\label{ssub:source_notations}


We assume the following pairwise disjoint countably infinite sets:
$\symDatasetUni$ (datasets), and  $\symDataObjUni$ (data objects).
Each dataset in $\symDatasetUni$ is a collection of data objects.
To denote that a data object~$\dataObject \in \symDataObjUni$ is contained in
such a dataset~$\symDataset \in \symDatasetUni$, we write
$\dataObject \in \symDataset$.

Furthermore, we introduce a countably infinite set $\symDataAccUni$ (data
access languages) in order to query the aforementioned data objects $\symDataObjUni$.
$\symDataAccUni$ is pairwise disjoint with $\symDataObjUni$ and $\symDatasetUni$.
Each data access language  $\symDataAcc \in \symDataAccUni$ is a set, where
every element $\queryExpr \in \symDataAcc$ is a one of the expressions of the
language.


Informally, a data object~$\dataObject \in \symDataObjUni$ is an abstract
entity that could be queried using a query expression to extract
one or more data objects \textbf{which can, in turn, be accessible by different data access languages}.
For example, a JSON object is considered a data object with JSONPath as the data
access language, and it can be further queried using JSONPath expressions to
extract JSON arrays (data objects) or a JSON field containing a string
representation of CSV rows (data objects), which can be further queried using a CSV specific
data access language.





\subsubsection{Source Type}%
\label{ssub:source_type}

\begin{definition}
	\label{def:source_type}
	A \textbf{source type} is a tuple $\sourceTypeTuple$ such that
	$\symSetDataset \subseteq \symDatasetUni$, $\symDataAcc \in \symDataAccUni$,
	$\eval$ is a partial function $\eval: \symSetDataset \times \symDataAcc \rightarrow \symDataSeqUni$,
	$\cbeval$ is a partial function $\cbeval: \symSetDataset \times \symDataObjUni \times \symDataAcc \rightarrow \symDataSeqUni$
	where $\symDataSeqUni$ is the set of all possible sequences of data objects in $\symDataObjUni$.

	\todo[inline]{For both $\eval$ and $\cbeval$, I wonder whether they should really be partial functions? Not total? Perhaps partial is okay for both: for $\cbeval$ it may be argued that some of the expressions in $\symDataAcc$ are not meant to be used as "global" (context object independent) queries, and the same type of argument can be used for $\cbeval$ as well. Moreover, for $\cbeval$ we may additional argue that likely not all data objects in $\symDataObjUni$ would be possible input parameters.}
\end{definition}


\begin{definition}
	\label{def:data_source}
	A \textbf{data source} is a tuple~$\dataSource = \dataSourceTuple$ such that
	$\sourceType = \sourceTypeTuple$ is a source type and
	$\symDataset \in \symSetDataset$.
\end{definition}


\begin{definition}
	\label{def:cast_function}
	A \textbf{type-cast} function is a partial function
	$\typeCast: \symDataObjUni \rightarrow \symAllLiterals$.
	%	For such a type-cast function~$\typeCast$ and every sequence $\symDataSeq$ of data objects, we write
	%	$\typeCast[\symDataSeq]$ to denote the sequence of literals and error
	%	values~$\error$ such that the $i$-th element in $\typeCast[\symDataSeq]$
	%	is $\fctTypeCast{\dataObject_i}$ if $\dataObject_i \in dom(\typeCast)$,
	%	and $\error$ otherwise, where $\dataObject_i$ is the $i$-th element in
	%	$\symDataSeq$, for all $i \in  \{1,\dots,|\symDataSeq|\}$.
	%	The order of the data objects is preserved.
\end{definition}

\begin{definition}
	\label{def:source_operator}
	Let $\dataSource = \dataSourceTuple$ be a data source with
	$\sourceType=\sourceTypeTuple$.
	Given a tuple~$\iterConfig = \iterConfigTuple$, where
	$\queryExpr_{root} \in \symDataAcc$ and
	$\symAttrQueryMap$ is a partial function
	$\symAttrQueryMap \!: \symAttrUniverse \setminus \{\fragAttr\} \rightarrow \symDataAcc$,
	the \textbf{$\iterConfig$-specific mapping relation obtained from
		$\dataSource$} is the mapping relation $\mappingRel$ such that
	\begin{align*}
		\symAttrSubSet  & = \fctDom{\symAttrQueryMap} \cup \{\fragAttr\}
		\; \text{ and}
		\\
		\symMappingInst & = \left\{ \{\attr_1 \rightarrow \fctTypeCast{\var_1}, \dots, \attr_n \rightarrow \fctTypeCast{\var_n}, \fragAttr \rightarrow \fragDefault \} \ | \
		\dataObject \in \symDataSeq_{root} \text{ and } ((\attr_1 , \var_1), \dots, (\attr_n , \var_n)) \in X_d\right\}
	\end{align*}
	where $\symDataSeq_{root} = \fctEval{\symDataset}{\queryExpr_{root}}$ and
	\begin{equation*}
		X_d = \bigtimes_{\attr \in \fctDom{\symAttrQueryMap}}
		\{(\attr, \var) \ | \
		\var \in \fctCbeval{\symDataset}{\dataObject}{\queryExpr} \text{ with }  q = \symAttrQueryMap(\attr)\}
		.
	\end{equation*}
\end{definition}







\subsection{Projection Operator}%
\label{sub:projection_operator}

The \textbf{projection} operator restricts the mapping tuples in a mapping
relation to a subset of the attributes of that relation.


\begin{definition}
	\label{def:tuple_restrict}
	Given a mapping tuple $\mappingTuple$ and a subset $A \subseteq
		\mathrm{dom}(\mappingTuple)$ of the attributes for which $\mappingTuple$ is
	defined, we write $\mappingTuple[A]$ to denote the mapping tuple
	$\mappingTuple'$ that is the restriction of $\mappingTuple$ to $A$;
	i.e., $\mathrm{dom}(\mappingTuple') = A$ and $\mappingTuple'(a) =
		\mappingTuple(a)$ for all $a \in A$.
\end{definition}

\begin{definition}
	\label{def:projection_operator}
	Let $r = \mappingRel$ be a mapping relation and $\symProjSet \subseteq
		\symAttrSubSet \setminus \{\fragAttr\}$ be a non-empty subset of the
	attributes of $r$.
	The \emph{$\symProjSet$-specific projection of $r$}, denoted by
	$\text{Project}^\symProjSet(r)$, is the mapping relation $(\symAttrSubSet',
		\symMappingInst')$ such that $\symAttrSubSet' = \symProjSet \cup
		\{\fragAttr\}$ and $\symMappingInst' = \{ \mappingTuple[\symAttrSubSet'] \ | \
		\mappingTuple \in \symMappingInst \}$.
\end{definition}




\subsection{Rename Operator}%
\label{sub:rename_operator}

{\color{red}
	\todoi{Change the following paragraph according to the definition - SMO}
	Given a mapping relation $\mappingRel$, and set of pairs of attributes
	$\symRenamePFunc$ with $\{(a_{1}, b_{1}),..(a_{n}, b_{n})\}$ where all $a$'s and
	$b$'s are unique.
	A \textbf{rename operator} renames the attributes in the mapping relations,
	$\mappingRel$,  according to set of the pairs of attributes~$\symRenamePFunc$.
	It creates a new mapping relation $(\symAttrSubSet', \symMappingInst')$
	such that $\symAttrSubSet'$ is a set of attributes where,
	given a pair of attributes $(a_{old},b) \in \symRenamePFunc$, for all
	attributes $a \in \symAttrSubSet$ is replaced with $b$ if $a = a_{old}$ otherwise
	$a$ from is kept in $\symAttrSubSet'$.
	For the \emph{renamed mapping tuples}~$\mappingTuple'$, it is the result of
	restricting the mapping tuple~$\mappingTuple$ to $\symAttrSubSet'$ as
	$\mappingTuple[\symAttrSubSet']$ with the condition such that for each
	pair $(a_{old},b) \in \symRenamePFunc$, we have $\mappingTuple'(b) = \mappingTuple(a_{old})$,
	and for every $a$ in $A$ that did not get renamed, we have $\mappingTuple'(a) =
		\mappingTuple(a)$, thus, $dom(\mappingTuple') = \symAttrSubSet'$.
}

\begin{definition}
	\label{def:rename_operator}
	Let $r = \mappingRel$ be a mapping relation.
	Given a partial function
	$\symRenamePFunc: (\symAttrSubSet \setminus \{\fragAttr\}) \rightarrow (\symAttrUniverse \setminus \{\fragAttr\})$
	that is injective,
	the \emph{$\symRenamePFunc$-specific renaming of $r$}
	is the mapping relation $(\symAttrSubSet'\!, \symMappingInst')$ such that
	\begin{align*}
		\symAttrSubSet'  & =  (\symAttrSubSet \setminus dom(\symRenamePFunc)) \; \cup \;
		\{ \symRenamePFunc(a) \ |  \ a \in dom(\symRenamePFunc) \} \; \text{ and }                                       \\
		\symMappingInst' & = \{ \fctRenameTuple{\symRenamePFunc}{\mappingTuple}\ | \ \mappingTuple \in \symMappingInst\}
	\end{align*}
	where, for every mapping tuple~$\mappingTuple \in \symMappingInst$, we write $\fctRenameTuple{\symRenamePFunc}{\mappingTuple}$ to denote the mapping tuple~$\mappingTuple'$ for which it holds that $\fctDom{\mappingTuple'} = \symAttrSubSet'$ and, for every attribute~$\attr \in \fctDom{\mappingTuple'}$,
	\begin{equation*}
		\mappingTuple'(\attr) =
		\begin{cases}
			\mappingTuple(\symRenamePFunc^{-1}(a)) & \text{if $\attr \in \mathrm{img}(\symRenamePFunc)$,} \\
			\mappingTuple(a)                       & \text{else.}
		\end{cases}
	\end{equation*}
\end{definition}

\subsection{Extend Operator}%
\label{sub:extend_operator}

\begin{definition}
	An \emph{extension function} is a function $\extFunc: \extFuncType \times \dots \times \extFuncType \rightarrow \extFuncType$.
	We denote the set of all possible \emph{extension function} as $\symExtFuncUni$.
\end{definition}

\begin{definition}
	An \emph{extend-expression}~$\extExpr$ is an expression that is defined recursively as
	follows:
\todo[inline]{The hyphen (-) in "extend-expression" is not correct English. In contrast, in English you don't need a hyphen there; i.e., it should just be "extend expression".}
\todo[inline]{Another general thing for all definitions: I see that you are using 'emph' to highlight the term that is being defined. I prefer to use 'textbf' instead because that makes the term stand out even more. 'emph' makes the text italic, but many of the math symbols in a definition are usually also in italic, which makes it a bit more difficult to immediately spot the defined term among all the italic text. With 'textbf' this problem goes away (assuming that usually nothing else in the definitions is presented in bold font).}
	\begin{enumerate}
		\item
		Every RDF term in $\symTermUniverse$ is an \emph{extend-expression}.
		\item
		Every attribute in $\symAttrUniverse$ is an \emph{extend-expression}.
\todo{Actually, we should rule out the fragment attribute, right?}
		\item
		If $\extExpr_1,\dots,\extExpr_n$ are extend expressions and
		$\extFunc \in \symExtFuncUni$, then the tuple $\extFuncTuple$ is an
		extend expression.
	\end{enumerate}
% 	Thus, a grammar that accepts such extend-expression $\extExpr$ is defined as
% 	follows where $x \in \symTermUniverse$, $a \in \symAttrUniverse$, and
% 	$\extFunc \in \symExtFuncUni$:
% 	\begin{equation*}
% 		\extExpr ::= x \ |\ a \ |\  (\extFunc, \extExpr, \dots, \extExpr)
% 	\end{equation*}
\end{definition}


\begin{definition}
	Let $r = \mappingRel$ be a mapping relation, $\attr \in \symAttrUniverse$ be
	an attribute such that $\attr \notin \symAttrSubSet$, and
	$\extExpr$ be an extend expression.
	The \emph{$\extExpr$-specific extension of $r$ for $\attr$} is the mapping tuple
	$(\symAttrSubSet'\!,\symMappingInst')$ where
	\begin{align*}
		\symAttrSubSet'  & = \symAttrSubSet \cup \{ \attr \} \text{ and }                                       \\
		\symMappingInst' & = \bigl\{\mappingTuple \cup \{\attr \rightarrow \fctEval{\extExpr}{\mappingTuple}\} \ | \
		\mappingTuple \in \symMappingInst \bigr\}
	\end{align*}
	where, for every mapping tuple~$\mappingTuple \in \symMappingInst$, $\fctEval{\extExpr}{\mappingTuple}$ is either an RDF term or the
	error value ($\error$), which is determined recursively as follows:

    \begin{enumerate}
        \item If $\extExpr$ is an RDF term $x$, then $\fctEval{\extExpr}{\mappingTuple} = x$.
        \item If $\extExpr$ is an attribute $\attr$ and $\attr \in \fctDom{\mappingTuple}$, then $\fctEval{\extExpr}{\mappingTuple} = \mappingTuple(\attr)$.
        \item If $\extExpr$ is an attribute $\attr$ and $\attr \notin \fctDom{\mappingTuple}$, then $\fctEval{\extExpr}{\mappingTuple} = \error$.
        \item If $\extExpr$ is a tuple $\extFuncTuple$ such that the extend function~$\extFunc$ is an $n$-ary function, then
            $\fctEval{\extExpr}{\mappingTuple} =  \fctExtFuncEvalN{\extExpr}{\mappingTuple}$.
        \item If $\extExpr$ is a tuple $\extFuncTuple$ such that the extend function~$\extFunc$ is not an $n$-ary function, then
            $\fctEval{\extExpr}{\mappingTuple} =  \error$.
    \end{enumerate}

    {\color{red}
        \begin{equation*}
            \fctEval{\extExpr}{\mappingTuple} = 
            \begin{cases}
                \extExpr & \text{if $\extExpr$ is an RDF term;} \\
                \mappingTuple(\extExpr) & \text{if $\extExpr \in \symAttrUniverse$ and $\extExpr \in \fctDom{\mappingTuple}$;} \\
                \fctExtFuncEvalN{\extExpr}{\mappingTuple}  &\ \parbox{15em}{if $\extExpr = \extFuncTuple$ such that $\extFunc$ is an $n$-ary function;} \\ 
                \error & \text{else.}
            \end{cases}
        \end{equation*}
    }


\end{definition}



\subsection{Fragmenter Operator}%
\label{sub:fragmenter_operator}

\todo[inline]{For simplicity, I would replace the function $\symFragmentPFunc$ by just a single such pair. This doesn't loose expressiveness because multiple fragmenter operators with different pairs may be stacked on top of one another.}
\todo[inline]{In any case, I wonder whether this definition is really capturing what you have in mind. The problem that I see is that, with this definition (and the other operators that we have so far), you cannot actually partition a mapping relation and say that some tuples go to some target and other go to a different target.}
\todo[inline]{And then I still find the name very confusing/misleading. For instance, without knowing anything, if I see $\mathrm{fragment}^{\symFragmentPFunc}(\mappingTuple)$, I would assume that this function somehow returns a fragment of the given tuple $\mappingTuple$, not that it changes a value in that tuple.}

\begin{definition}
	\label{def:fragmenter_operator}
	Let $r=\mappingRel$ be a mapping relation, and a set of pairs
\todo{Instead of "set of pairs", shouldn't that be "partial function"?}
	$\symFragmentPFunc:  \symFragmentSet \rightarrow \symFragmentSet$
	where $\fctDom{\symFragmentPFunc} \cap \mathrm{img}(\symFragmentPFunc) = \emptyset$.
	The \emph{$\symFragmentPFunc$-specific fragmentation of $r$} is
	a mapping relation $(\symAttrSubSet', \symMappingInst')$ such that
	$\symAttrSubSet' =  \symAttrSubSet$ and
	\begin{equation*}
		\symMappingInst'=
		\{ \mathrm{fragment}^{\symFragmentPFunc}(\mappingTuple) \ | \
		\mappingTuple \in \symMappingInst
		\}
	\end{equation*}
	where, for every mapping tuple $\mappingTuple \in \symMappingInst$,
	we write $\mathrm{fragment}^{\symFragmentPFunc}(\mappingTuple)$
	to denote the mapping tuple $\mappingTuple'$ for which it holds
	that $\fctDom{\mappingTuple'} = \symAttrSubSet'$ and,
	for every attribute $a \in \fctDom{\mappingTuple'}$,
	\begin{equation*}
		\mappingTuple'(a) =
		\begin{cases}
			\symFragmentPFunc(\mappingTuple(a)) & \text{ if } a = \fragAttr,
			\mappingTuple(a) \in \fctDom{\symFragmentPFunc}                  \\
			\mappingTuple(a)
		\end{cases}
	\end{equation*}
\end{definition}


\subsection{$\theta$-join Operator}%
\label{sub:theta_join_operator}

\todo[inline]{It is not actually a $\theta$-join if the only comparison operator between the join variables is equivalence. Then, it is just an equijoin (which is a special case of $\theta$-joins). See the Wikipedia link in the LaTeX comment here:} % https://en.wikipedia.org/wiki/Relational_algebra#%CE%B8-join_and_equijoin


\begin{definition}
	\label{def:theta_join_operator}
	Given
\todo{This is not a complete English sentence. Sentences that start with "Given" are always of the form: "Given x, y." You have the "x" part here, but not the "y" part.}
	two mapping relations $r_{left} = \mappingRelSpec{left}$ and
	$r_{right} = \mappingRelSpec{right}$, and a set of pairs of attributes
\todo{Shouldn't $\symJoinAttrPairs$ be a subset of $\symAttrSubSet_{left} \times \symAttrSubSet_{right}$?}
\todo{Also, can the fragment attribute part of any such pair?}
	$\symJoinAttrPairs: \symAttrSubSet_{left} \times \symAttrSubSet_{right}$.
	We assume
\todo{You cannot state this as an assumption (we are not making assumptions as part of formal definitions). Instead, you must state this as a condition for the pair $r_{left}$ and $r_{right}$.}
	that $\symAttrSubSet_{left} \cap \symAttrSubSet_{right} = \{\fragAttr\}$.
	The \emph{$\symJoinAttrPairs$-specific join of mapping relations $r_{left}$ and
		$r_{right}$} is the mapping relation $(\symAttrSubSet', \symMappingInst')$
	such that $\symAttrSubSet' = \symAttrSubSet_{left} \cup \symAttrSubSet_{right}$
	and
	\begin{equation*}
		\symMappingInst' =  \{ \mappingTuple' \cup \mappingTuple'' \ | \
		\mappingTuple' \in \symMappingInst_{left} \text { and }
		\mappingTuple'' \in \symMappingInst_{right} \text{ such that }
		\mappingTuple'(\attr') = \mappingTuple''(\attr'') \text{ for all }
		(\attr',\attr'') \in \symJoinAttrPairs
		\}
	\end{equation*}
\end{definition}

\todo[inline]{What if $t'$ and $t''$ have a different value for the fragment attribute?}










