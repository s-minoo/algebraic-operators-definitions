\section{Preliminaries}
\label{sec:preliminaries}
In this section, we provide a preliminary definition of terms which will be
used throughout the rest of this paper in the definitions of
the algebraic mapping operators.

We assume the following pairwise disjoint countably infinite sets:
$\symAttrUniverse$ (attribute names), $\symAllIRIs$ (IRIs), $\symAllBNodes$
(blank nodes), $\symAllLiterals$ (literals), and $\symFragUniverse$ (\fragment{}
names).
We define a set of terms $\symTermUniverse$ as $\symTermUniverse = \symAllIRIs \cup
	\symAllBNodes \cup \symAllLiterals$.
Furthermore, we also define a special value $\error$ with
$\error \notin (\symTermUniverse \cup \symAttrUniverse \cup \symFragUniverse)$,
we let $\fragAttr \in \symAttrUniverse$ be a designated attribute called the \emph{fragment attribute}, 
and $\fragDefault \in \symFragUniverse$ be a designated \emph{default} fragment value.

Using the aforementioned sets, we define the data model on which our algebraic
mapping operators work: \emph{a mapping relation}.
We build up the definition of a mapping relation in two steps as follows:


\begin{definition}
	A \emph{mapping tuple} is a partial function $\mappingTuple:
		\symAttrUniverse \rightarrow \symFragUniverse \cup \symTermUniverse \cup \{\error\}
	$.
\end{definition}

\begin{definition}
	A \emph{mapping relation} is a tuple $\mappingRel$, where
	$\symAttrSubSet \subset \symAttrUniverse$ is a finite, non-empty set of attributes
	such that $\fragAttr \in \symAttrSubSet$ and
	$\mappingInst$ is a set of mapping tuples such that
	$\mathrm{dom}(\mappingTuple) = \symAttrSubSet$ for every $\mappingTuple$ in $\mappingInst$, % i.e., each mapping tuple in $\mappingInst$ is defined for $\symAttrSubSet$
	$\fctMappingTuple{\fragAttr} \in \symFragUniverse$, and
	$\fctMappingTuple{a} \in \symTermUniverse \cup \{\error\}$ for all $a \in \symAttrSubSet \setminus \{\fragAttr\}$.
	We call $\symAttrSubSet$ the \emph{schema} of the mapping relation
	and $\mappingInst$ is the \emph{instance} of the mapping relation.
	\label{def:mapping_relation}
\end{definition}


We also define the restriction of the domain of the mapping tuples to make the
definitions of the algebraic mapping operators more accessible to read.

\begin{definition}
	\label{def:tuple_restrict}
	Given a mapping tuple $\mappingTuple$ and a subset $A \subseteq
		\mathrm{dom}(\mappingTuple)$ of attributes for which $\mappingTuple$ is
	defined, we write $\mappingTuple[A]$ to denote the mapping tuple
	$\mappingTuple'$ that is the restriction of $\mappingTuple$ to $A$;
	i.e., $\mathrm{dom}(\mappingTuple') = A$ and $\mappingTuple'(a) =
        \mappingTuple(a)$ for all $a \in A$.
\end{definition}


